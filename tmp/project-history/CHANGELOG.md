# 更新日志（CHANGELOG）

格式参考 Keep a Changelog；版本号遵循语义化版本。

## [Unreleased] - 2026-09-07

### Changed

- 修复 P0 存储兼容：`@system.storage` 写入统一在平台适配层 JSON 序列化为字符串，读取自动还原；设置、骰子模式与历史记录可跨启动恢复。
- 修复 P0 多骰结果可核对性：首页在 2D6、3D6 与自定义多骰结果中显示“单颗点数之和 = 总和”，不再只显示总数。
- 修复 P1 主题一致性：科技蓝、极客绿与赛博紫会同步作用于首页结果数字和功能页当前模式选择态。
- 修复 P1 生命周期：离开或销毁首页时取消尚未完成的掷骰状态机，确保不会在后台生成结果、写历史或触发震动。
- `.gitignore` 更新为公开仓库规则：忽略本地构建产物、RPK、签名材料、IDE 状态及日志；保留源码、设计图、上架素材、测试和需求记录。
- 将设计图从 `00_reference/design/` 移至根目录 `design/`，删除空的 `00_reference/`；README 改为功能效果、效果图、交互、适配及开发编译说明，并移除 AGENTS/工程结构说明。
- 新增 `publish/` 上架素材目录：提供 192×192 应用图标、600×400 的 3:2 应用封面，以及首页待掷、投掷结果、功能、设置、关于五张 320×643 截图；均不参与 RPK 打包。
- 应用图标按用户确认的骰子参考图提取重绘，移除文字及外围背景，替换 `src/common/images/icon.png`（192×192）；保留原 manifest 图标路径。
- 摇腕触发后最长 3 秒强制结算，途中停顿再摇不重置截止时间；强制结算后持续摇动被锁定，收到停稳事件后才允许下一次摇腕触发。结算仅生成一次结果并调用一次震动，清理超时与收束定时器。
- 历史卡片按参考图改为单行“结果 / 骰子模式 / 时间”：中间模式垂直居中，模式与时间统一为灰色 #8b9397，去掉多骰加法明细行；保留原有结果及历史数据。
- 小屏首页功能入口再上移 10 设计 px：底部锚点由 64px 改为 74px，提示锚点与骰子区域预留由 130px 改为 140px；大屏布局不变。
- 大小屏媒体查询分界统一由 160dp 改为 120dp，避免 336px/340dpi 的大屏（约 158dp）命中小屏规则，从而覆盖首页底边距与定位。首页和共享样式采用相同分界，仍不判断具体型号。
- 大屏首页功能卡底边距由 16px 调整为 8px，下移 8 设计 px；小屏底部锚点保持 64px。明确运行时仅按宽度区分大小屏，不按型号分支。
- 技术路线收敛为 Vela 原生页面 UI 加平台无关的 CommonJS 逻辑模块：首页骰子、结果、提示和“功能”入口样式改由 `pages/home/home.ux` 独立持有，不再通过共享样式或 `transform` 影响关键 Flex 布局。矩形大屏入口维持原始 16px 底边距；胶囊屏使用独立安全边距。
- 胶囊小屏首页的功能入口与提示改为 Vela 原生绝对定位：卡片固定锚定于 48px 底部安全区，骰子区域预留 114px，解决 Flex 内容总高度溢出导致卡片被圆角遮挡/完全落出可视屏幕的问题。
- 根据小屏虚拟设备反馈，功能入口进一步上移 16px：底部锚点调整为 64px，提示与骰子区域预留同步调整为 130px；矩形大屏不受影响。
- 应用包标识由 `com.vibecoding.cyberdice` 更改为 `com.cyberdice`；产品展示名称继续为 CyberDice。
- 曾尝试调整矩形大屏首页“功能”卡的位置，现按设计决策恢复为原始 16px 底边距；胶囊小屏的 80px 安全边距保持不变。
- 主题选择页改为三组静态条件节点与固定颜色类，避免 Vela 复用循环节点后科技蓝的选中底色残留在其他主题项；同时移除未使用的平台导入。
- 设计参考图从临时目录迁至 `00_reference/design/cyberdice-ui-reference.png`，采用稳定文件名；README 直接展示该图并说明其与动态运行时结果的关系。
- 清理非发布调试遗留：移除浏览器模拟器、归档的 AIoT 模板、未引用旧组件、旧模糊骰子资源及浏览器 QA 报告；保留 Vela 单元测试、需求基线、设计参考图和变更历史。README 重写为当前 AIoT IDE / RPK 工作流并直接展示设计参考图；`.gitignore` 补齐 Vela/AIoT 本地状态、RPK 和日志规则。
- 首页 READY 状态的上滑改为直接进入“历史记录”；右滑仍进入“功能”，左滑继续交由系统执行退出/返回。
- “历史记录”从功能页移除，统一由首页上滑进入，避免同一目的地存在重复入口。
- 重做多设备视觉尺寸：9 Pro / 10 Pro 的静止骰子与摇动帧提升至 184px、结果环提升至 276px；以 Vela 官方的 `max-width: 160dp` 媒体查询精确覆盖 9 / 10（96 / 106dp），使用居中顶部安全区、164px 骰子、224px 结果环、固定可见的底部功能入口及 64px 紧凑功能/历史卡片。
- 修复大屏摇动时 image 节点连续更新偶发不刷新的问题：摇动帧交替重建独立图片节点；小屏底部“功能”入口上移以避开圆角不可触达区域，功能/历史卡片分别提升至 76px / 72px（大屏为 80px / 78px）。
- 小屏首页“功能”入口进一步大幅上移：底部安全距由 30px 调整为 80px，提升胶囊屏下半区的可达性。
- 矩形大屏首页的“功能”卡恢复原始 56px 高度与 16px 底边距；小屏的上移规则不受影响。
- 功能页的 1D6 / 2D6 / 3D6 / 1D20 改为独立条件选中节点，规避 Vela 列表节点复用使 1D6 选中蓝标滞留的问题；卡片高度由 68px 提升至 76px，入口区域支持纵向滚动。

- 对齐 AIoT-IDE/Vela 工具链的 manifest 路由格式（入口与页面键使用 `pages/...`），保留 Vela 原生 `router.push` 所需的 `/pages/...` URI；补齐已实际使用的 `system.router`、`system.storage` 能力声明。页面共享样式改为 Vela 编译器可解析的 CSS `@import`，使正式源码可由官方构建器编译并部署到模拟器。
- 合并 AIoT-IDE 创建的重复模板工程：根目录吸收官方构建/调试脚本及依赖和 `config-watch.json`，模板将归档至 `tmp/`，避免存在两套同名源码与构建入口。
- Git 忽略本地 AIoT-IDE 配置和依赖目录；构建产物仍只保留目录占位文件，调试 RPK 不提交。
- manifest 补齐 AIoT-IDE 模板要求的 `deviceTypeList: ["watch"]` 声明，确保官方穿戴设备运行时按手表应用初始化页面样式与路由。
- 页面访问应用级 store 时统一使用 Vela 官方运行时提供的 `$app` 对象属性，替代不受支持的 `$app()` 调用，修复首页生命周期启动异常。
- 工程目录按发布与源码职责重组：新增根目录 `build/`、`dist/`、`sign/`；运行时图片迁至 `src/common/images/`，JavaScript 模块迁至 `src/common/scripts/`，共享样式迁至 `src/common/styles/`；预留 `src/common/components/` 和 `src/i18n/`。
- 所有页面、manifest、应用入口、单元测试、模拟器和资产生成器均已更新至新路径；模拟器、生成工具、需求说明、概念图和变更历史归档到 `tmp/`。
- 重写根目录 README 和 AGENTS 指引，明确发布物、签名材料、离线边界、测试位置与归档规则；不生成或提交伪造的 PEM 签名文件。
- 新增 Git 忽略规则与构建、发布、签名目录占位文件；构建产物、macOS 元数据和 PEM 签名材料不会进入版本控制。
- 首页移除不适合手环触控的 20px 历史/设置图标；READY 状态仅右滑打开“功能入口”，左滑完全留给系统退出、上下滑不绑定功能，且掷骰期间禁止切页。
- 多骰结果改为“单骰表达式 + 大号总和”，设置项支持整行点击；传感器订阅恢复后增加 600ms 基线稳定窗口，并明确显示点击兜底状态。
- 骰子资产升级为 297px 静止面和四帧 297px 残影帧；摇动只切换确定性的视觉帧，最终结果仍只由 `DiceEngine` 在 RESULT 状态生成。结果刻度环收敛为 8 段蓝色刻度，贴近概念图的留白节奏。
- 新增可复用的 `page-header`、`list-row`、`dice-stage` Vela 组件；右滑功能入口内提供骰子模式、偏好设置与历史记录三类大行入口。
- 移除所有应用内页头返回按钮，统一保留系统左滑返回；模拟器功能列表隐藏浏览器右侧滚动轨道，滚动能力仍保留给内容溢出时使用。
- 移除所有页面右上角时间及其刷新定时器，顶栏只保留页面名称或应用名称。
- 浏览器模拟器改为有限时长的传感器校准/手势采样，空闲时停止 50Hz 轮询；首页右滑手势增加 Pointer Event、触屏后备、方向锁定与取消处理。
- 模拟器首页上滑进入历史记录、右滑进入功能入口；左滑在子页面模拟系统返回，首页仍不拦截退出手势。
- 浏览器模拟器在右侧“交互”控制面板提供“模拟上滑：历史”“模拟右滑：功能”“模拟左滑：返回”按钮，复用同一套手势路由；手环画面与应用源码不受此项影响。
- 以用户确认的概念图为源，提取 READY 骰子和四帧摇动残影并接入手环应用与模拟器；结果仍动态显示 DiceEngine 生成的数字和概念图风格的蓝色八段刻度环。
- 结果态移除多骰加法明细，仅保留刻度环中的最终数字，与概念图的结果层级一致。
- “关于”页改用概念图同源的白色骰子资产，并将版本与引导语统一为中文（“版本 v1.1.0”“摇一摇，随时掷骰”）。
- 首页右滑后的“功能入口”改名为“功能”；“关于”同时作为功能页的直接入口保留，无需先进入设置。
- 修复以 `file://` 直接打开模拟器时骰子图片偶发/无法显示的问题：图片改为相对路径，模拟器纯逻辑模块预打包为本地脚本，不再依赖受浏览器限制的本地 `fetch`。
- 新增小米手环 9（192×490 胶囊屏）与手环 10（212×520 胶囊屏）体验版适配：manifest 声明 336px 设计基准、页面根容器改为全屏 Flex、胶囊屏使用安全边距媒体查询；模拟器可切换 9 / 9 Pro / 10 三个真实尺寸档。9 Pro 仍为正式基线，9 与 10 需真机验收后才能转为正式支持。
- 新增小米手环 10 Pro（336×480 矩形屏）体验版档；其与 9 Pro 同分辨率，复用矩形布局，并在模拟器、兼容计划和需求基线中登记。仍需独立完成真机安装、传感器与连续掷骰验收。
- 移除模拟器手动机型选择；改为根据浏览器可用屏幕尺寸自动匹配手环 9 或 10，未匹配时回退至 9 Pro 的 336×480 正式基线。实际 RPK 始终由 Vela `designWidth` 和屏幕形状媒体查询自动适配。
- 官方 AIoT IDE 模拟器调试兼容：路由使用 Vela 所需的无前导斜杠注册键，页面样式改由 `<style>@import</style>` 编译，并为首页初始化添加服务异常降级，避免平台服务不可用时出现整屏空白。
- 补充 AIoT IDE 新建工程同款 `jsconfig.json`，使 UX 语言服务完成初始化；重新打开根目录后，IDE 顶部可直接使用选择设备、调试、打包和发布命令。
- 修复浏览器模拟器与官方 Vela 包的视觉分叉：骰子舞台、页面标题和复用列表行改为在各自 `.ux` 组件内声明样式。浏览器单页 CSS 曾掩盖 Vela 的组件样式隔离，导致官方包缺失骰子与部分导航视觉。
- 新增 Vela 组件视觉一致性回归测试，校验骰子、结果环、标题和列表行的关键样式不会再次只存在于浏览器模拟器。
- 功能页与历史页进一步去除跨组件/`show` 渲染差异：功能入口改为与浏览器模拟器完全一致的页面内卡片结构；历史空态、列表和“清空”操作改用 Vela 稳定的条件块渲染。
- 修复官方 Vela 包的应用单例访问：页面从 `this.$app.$def` 获取 app 级 store/controller，恢复首页骰子、摇动动画、结果、历史记录和功能入口；同时为列表、空状态和骰子舞台声明 Vela 必需的纵向 Flex 布局，避免卡片横排。
- 首页骰子舞台改为页面内直接结构，绕开 Vela 自定义组件宿主未继承 Flex 尺寸导致的骰子区域高度为零；功能卡片行高从 60px 调整至 68px，提升触控友好度。

## [1.1.0] - 2026-09-06

依据用户提供的 UI 概念图完成前端全面重构（图标 / UI / 交互），功能逻辑保持 v1.0 验收基线。

### Added

- 全新视觉：纯黑背景 + 深灰卡片 + 蓝色点缀 + 白色 3D 等距骰子。逐像素光栅化资产生成器 `tools/gen-assets.js`（依赖 `tools/png.js`，`node tools/gen-assets.js` 重新生成）：
  - `assets/icon.png` 概念图风格应用图标；`assets/dice/die-1..6.png` 六帧 3D 骰子（正等轴投影 + 形态学开运算圆角六面体 + 棱线亮度柔化）；`die-blur.png` 摇动模糊帧；`result-ticks.png` 结果页蓝色刻度线；时钟/齿轮/文档小图形。
- 多页面导航（`@system.router`）7 屏：首页、选择骰子、自定义骰子、历史记录、设置、主题、关于。
- 新骰子模式：3D6 预设；自定义骰子（数量 1~6 × 面数 4/6/8/12/20/100）。
- 历史记录：最近 20 次投掷（值 / 模式 / 加法明细 / 时间），持久化存储，空状态引导，一键清空。
- 设置页：震动反馈、动画效果开关（即时生效并持久化）；主题页三种强调色（科技蓝 / 极客绿 / 赛博紫）。
- 关于页：骰子图形 + CyberDice + 版本 + 「Shake. Roll. Anywhere.」。
- `common/store.js`：跨页面共享状态（设置/模式/历史/掷骰状态广播）+ 持久化；`common/storage-adapter.js`：`@system.storage` 封装（不可用时内存兜底）。
- 掷骰控制器提升到 app 层（`app.ux`），页面导航不中断掷骰流程；结果自动写入历史。
- 单元测试扩充至 32 个用例：新增 3D6 / 自定义 / normalizeMode 边界 / store 持久化行为。

### Changed

- 首页交互重构：顶栏（应用名 + 时钟）+ 历史/设置入口图标；骰子区域点击/摇腕掷骰不变；底部模式卡片进入选择骰子页；结果页改为大数字 + 蓝色刻度线（多骰显示总和与加法明细）。
- 摇骰动画改为模糊帧 / 随机帧交替 + 左右抖动，"动画效果"关闭时直接显示结果。
- manifest：新增 7 个路由页面，版本升至 1.1.0（versionCode 2），背景色纯黑。
- 全部尺寸比例按概念图校准（顶栏 52 / 列表行 60 / 主字号 24 / 骰子 148 / 结果刻度环 240）；修正骰子小尺寸下"压扁六边形"观感（圆角六面体 + 棱线柔化渲染）。

### Fixed

- `storage-adapter` 误把 `@system.storage.get` 的回传值当 `{value}` 包装，导致读取恒为 null 的真机隐患。
- 模拟器浏览器版存储签名与适配器不一致，造成所有持久化写入集中在坏键、重载后设置/历史丢失。
- 自定义骰子页步进行标签挤压竖排（改为标签在上、控件在下）。

## [1.0.0] - 2026-09-06

首个功能完整版本，对应说明书 v1.0 实施基线。

### Added

- 小米手环 9 Pro（336×480）Vela JS 快应用工程：`src/manifest.json`（声明 `system.sensor` / `system.vibrator` 能力）、`app.ux`、主掷骰页面 `pages/index/index.ux`。
- 1D6 / 2D6 / 1D20 三种模式，点击底部按钮切换；摇动过程中禁止切换（说明书 7.3 节）。
- 核心逻辑模块（`src/common/`，平台无关、可注入时间与定时器便于测试）：
  - `dice-engine.js`：随机结果生成；2D6 独立双骰，非 2~12 直接抽样。
  - `shake-detector.js`：自适应基线 EMA + 峰值窗口摇动识别（SHAKE_START / SHAKE_END）。
  - `roll-controller.js`：READY→SHAKING→SETTLING→RESULT 状态机，防重入、结果冷却、最短动画时长。
  - `sensor-adapter.js`：`@system.sensor` 封装，幂等订阅、失败回调、game 频率。
  - `feedback-service.js`：`@system.vibrator` 封装，结果锁定短震一次、异常吞掉。
  - `constants.js`：阈值与时长常量（真机调参入口）。
- 页面生命周期管理：onHide/onDestroy 取消传感器订阅与动画定时器，onShow 重置 Detector 并重订阅。
- 传感器不可用时点击兜底，异常不阻塞 UI（FR-010）。
- 单元测试 28 个用例（`tmp/test/`，无第三方依赖，`node tmp/test/run.js`）：DiceEngine 范围/分布、ShakeDetector 静止/抬腕/步行不触发与单次触发、dt=0 样本、10 万样本吞吐冒烟、RollController 状态机/防重入/冷却/中途销毁、Lifecycle 订阅幂等。
- 浏览器模拟器（`sim/`，336×480）：通过 CommonJS shim 复用 `src/common` 真实逻辑；mock 传感器 20ms 持续回流，支持注入摇腕/步行/抬腕波形、传感器故障、onHide→onShow 生命周期与诊断日志。
- 应用图标 `src/assets/icon.png`（`tools/gen-icon.js` 生成）。
- 文档：README、CHANGELOG、AGENTS.md。

### Verified

- 单元测试 28/28 通过。
- 模拟器实测 11 项场景全部通过：三模式点击掷骰、摇腕链路单次触发、步行/抬腕零误触、冷却防重入、传感器故障降级、生命周期恢复、100 次连续掷骰压测。
- 深度验证：状态机 2000 次连续长跑（结果:震动 = 2000:2000、零卡死、平均 77.8ms/次）；ShakeDetector 单样本 0.03µs；3 分钟混合信号浸泡（21 次注入、7 次摇腕全部命中、步行/抬腕零误触、堆内存 4.6→4.8MB 无增长、全程零 JS 错误）。

### Fixed

- 模拟器模块 shim 键名不一致导致初始化中断（`require('./constants.js')` 解析失败）。
- 模拟器信号流在手势结束后停流，与真机持续回流不符导致 SHAKE_END 无法产生；改为常开信号流 + 波形叠加。
- 模拟器波形幅度三轴均分导致模长缩为 m/√3，制造人为误触发；修正为 m/√3。
- SensorAdapter 平台同步失败时订阅标志被覆盖为已订阅的问题。

### Changed

- 应用更名：Wrist Dice → 赛博dice → **CyberDice**；manifest 应用名与包名（`com.vibecoding.wristdice` → `com.vibecoding.cyberdice`）、模拟器、文档同步更新。项目目录由 `WristDice/` 更名为 `CyberDice/`。

### 说明

- 真机项（9 Pro 安装渠道、真机阈值标定、FMP 实测、release RPK 签名）待具备测试设备后执行，测试组见说明书 16.3 节。

# Design QA — CyberDice dice states

## Comparison target

- Source visual truth: `tmp/reference/design/e31c58c5-5586-4229-88c4-8791c72703df.png` (1536 × 1024 RGB).
- Implementation: in-app browser rendering of `http://127.0.0.1:8931/tmp/sim/index.html`, 336 × 480 CSS-px device content at density 1. Browser capture was made in the in-app browser (tab 11) for READY and SHAKING; it is not persisted as a file.
- Normalization: comparison excludes the presentation board, desktop control panel, and watch bezel. The source's watch-screen artwork is scaled to the simulator's 336 × 480 content canvas.

## Comparison history

1. Earlier state: procedural static dice and generic residue frames had the same broad style but did not reproduce the source's exact face lighting and motion blur.
2. Fix: `tmp/tools/gen-assets.js` now extracts the approved source artwork into one 296px READY asset and four 296px motion crops. The app and simulator use those assets directly.
3. Post-fix evidence: the browser READY capture shows the identical white rounded D6 crop; the SHAKING capture shows the source's layered grey/black trailing ghosts. Result remains a dynamic number inside the existing source-matched blue eight-tick PNG so values are not baked into an image.

## Required fidelity surfaces

- Fonts and typography: READY and SHAKING retain the existing high-contrast white/gray hierarchy. RESULT uses one large final number only, matching the source's single-number hierarchy rather than adding a small dice expression.
- Spacing and layout rhythm: dice stays centered in the 148px visual slot; shaking residue is cropped at 296px and displayed at 148px, so it preserves the source's framing without overflow.
- Colors and visual tokens: black screen, off-white dice facets, gray motion ghosts, white result number, and blue tick accents align with the reference.
- Image quality and asset fidelity: READY and SHAKING artwork is extracted from the user-approved source, not approximated with CSS or new illustration. All five display assets are 2× raster files.
- Copy and content: state copy remains `摇一摇手腕 或 点击掷骰` and `摇动中…`; the final result is generated by DiceEngine and is always directly readable as a number.

## Findings

- No actionable P0/P1/P2 visual mismatch remains in the requested dice, motion, or result surfaces.
- P3: physical Band 9 Pro rendering should still be checked for panel color and image resampling, because browser rasterization cannot prove device-panel output.

## Verification

- `node tmp/tools/gen-assets.js` regenerated assets from the source design.
- `npm test`: 38/38 passed.
- Browser exercised READY, the full shake state machine, and the new source-derived shaking frame. No browser-visible runtime error occurred.

## Final result

final result: passed
